% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{ont_connect}
\alias{ont_connect}
\title{Connect to an Ontology Database}
\usage{
ont_connect(path = "ontology.duckdb", read_only = FALSE, init = TRUE)
}
\arguments{
\item{path}{Path to DuckDB database file. Use \code{":memory:"} for an
in-memory database (useful for testing). Default creates a file
called \code{"ontology.duckdb"} in the current working directory.}

\item{read_only}{Logical. If \code{TRUE}, connect in read-only mode.}

\item{init}{Logical. If \code{TRUE} (default), initialize schema if tables
don't exist.}
}
\value{
Invisibly returns the connection object. The connection is also
stored internally for use by other \verb{ont_*} functions.
}
\description{
Creates or connects to an ontology database. The database stores all
ontology metadata: object types, link types, concepts, versions, audits,
and governance logs.
}
\examples{
# In-memory database for testing
ont_connect(":memory:")

# Persistent database
ont_connect("my_ontology.duckdb")

# Check connection status
ont_status()

# Disconnect when done
ont_disconnect()

}
