% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{ont_define_score}
\alias{ont_define_score}
\title{Define a Composite Score}
\usage{
ont_define_score(
  score_id,
  score_name,
  object_type,
  components,
  aggregation = "weighted_sum",
  description = NULL,
  score_range_min = 0,
  score_range_max = 100,
  thresholds = NULL,
  con = NULL
)
}
\arguments{
\item{score_id}{Unique identifier for this score.}

\item{score_name}{Human-readable name.}

\item{object_type}{Which object type this score applies to.}

\item{components}{List of component definitions, each with: concept_id, scope, weight, etc.}

\item{aggregation}{Aggregation method: "weighted_sum", "weighted_avg", "max", "min",
"any_true", "all_true", "count_true".}

\item{description}{Optional description.}

\item{score_range_min}{Minimum possible score (default 0).}

\item{score_range_max}{Maximum possible score (default 100).}

\item{thresholds}{Named list of thresholds for tier assignment (e.g., list(low=30, medium=60, high=80)).}

\item{con}{Optional DBI connection.}
}
\value{
Invisibly returns the score_id.
}
\description{
Creates a composite score that aggregates multiple concepts into a single metric.
}
\examples{
\dontrun{
ont_define_score(
  score_id = "patient_risk_score",
  score_name = "Patient Risk Score",
  object_type = "Patient",
  components = list(
    list(concept_id = "fall_risk", scope = "clinical", weight = 0.3),
    list(concept_id = "readmission_risk", scope = "clinical", weight = 0.4),
    list(concept_id = "medication_complexity", scope = "pharmacy", weight = 0.3)
  ),
  aggregation = "weighted_sum",
  thresholds = list(low = 30, medium = 60, high = 80)
)
}

}
