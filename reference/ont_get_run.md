# Get Run Details

Retrieves details for a specific run.

## Usage

``` r
ont_get_run(run_id, con = NULL)
```

## Arguments

- run_id:

  The run identifier.

- con:

  Optional DBI connection.

## Value

A list with run details, or NULL if not found.
