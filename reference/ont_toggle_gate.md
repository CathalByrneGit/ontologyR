# Enable/Disable Gate

Toggles a gate's enabled status.

## Usage

``` r
ont_toggle_gate(gate_id, enabled, con = NULL)
```

## Arguments

- gate_id:

  The gate identifier.

- enabled:

  TRUE to enable, FALSE to disable.

- con:

  Optional DBI connection.

## Value

Invisibly returns TRUE.
